# Copie este arquivo para .env e substitua os valores
# Chave API do SendGrid (opcional). Se não informar, o backend vai simular o envio.
SENDGRID_API_KEY=
# Remetente que aparecerá no e-mail enviado (ex: "Meu App <no-reply@meudominio.com>")
FROM_EMAIL=no-reply@example.com
# Porta do servidor (padrao 3000)
PORT=3000
